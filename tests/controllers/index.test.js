describe('Controller', () => {
  // Prepare service mock
  describe('Successful', () => {
    // Mock successful response from service
    it('should return valid temperature for a valid city', async () => {
      // Verify the service is being called with the right parameters
    });
  });

  describe('Failed', () => {
    
    it('should throw an error when city is not provided', async () => {
      
    });

    // Mock failure from service
    it('should throw an error when service fails', async () => {
      
    });
  });

  
});
