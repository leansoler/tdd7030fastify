describe('Weather API', () => {
  describe('Succesful response', () => {
    // Mock the HTTP library to return a valid result
    
  });

  describe('Failed response', () => {
    // Simulates an undefined city
    it('should return undefined when no city is provided', async () => {
      // Verify the HTTP library is NOT called
    });

    // Mock the HTTP library to return an invalid result
    it('should throw an error when http fails', async () => {
      
    });
  });
});
