describe('Routes', () => {
  // Prepare controllers mock
  describe('Successful', () => {
    // Mock successful response from controller
    it('should return valid temperature for a valid city', async () => {
      // Verify controller is called with the right parameters
    });

    it('should use default city when city is not provided', async () => {
    // Verify controller is called with the right parameters
    });
  });

  describe('Failed', () => {
    // Mock failure from controller
    it('should throw an error when controllers fails', async () => {});
  });
});
